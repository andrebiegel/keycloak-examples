ServerName shibboleth

<VirtualHost *:80>
    ServerName http://shibboleth
    UseCanonicalName On

    DocumentRoot "/var/www/html"

    <Location /secured>
        AuthType shibboleth
        ShibRequestSetting requireSession 1
        require shib-session
    </Location>
    
</VirtualHost>